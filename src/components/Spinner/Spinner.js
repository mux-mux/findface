import React from 'react';

import './Spinner.css';

const Spinner = () => {
  return (
    <div className="loader-container">
      <div class="loader"></div>
    </div>
  );
};

export default Spinner;
